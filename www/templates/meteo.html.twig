<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8">
        <link rel="shortcut icon" href="/meteo/favicon.ico" type="image/x-icon">
        <title>{% block title %}Метеостанция c.Усть-Иша{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('meteo') }}
        {% endblock %}
    </head>
    <body>
    {% if app.environment == 'prod' %}
        {% include './meteo/analytic.html.twig' ignore missing %}
    {% endif %}
    <div id="root"></div>
    {% block javascripts %}
        {{ encore_entry_script_tags('meteo') }}
    {% endblock %}
    </body>
</html>
